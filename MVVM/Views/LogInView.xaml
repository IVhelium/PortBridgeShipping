<UserControl x:Class="PortBridgeShipping.MVVM.Views.LogInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <Grid Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center" Width="420">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Log In" FontSize="22" HorizontalAlignment="Center" />

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock Text="Username" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="30" />

            <TextBlock Text="Password" Margin="0,8,0,0" />
            <PasswordBox x:Name="PasswordBox" Height="30" PasswordChanged="PasswordBox_PasswordChanged" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="Login" Width="100" Command="{Binding LoginCommand}" Margin="0,0,10,0" />
            <Button Content="Register" Width="100" Command="{Binding RegisterCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="6" Text="{Binding Message}" Foreground="DarkRed" HorizontalAlignment="Center" />
    </Grid>
</UserControl>
