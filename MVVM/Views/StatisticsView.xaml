<UserControl x:Class="PortBridgeShipping.MVVM.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:PortBridgeShipping.MVVM.ViewModels"
             Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <vm:StatisticsViewModel />
    </UserControl.DataContext>

    <StackPanel Margin="10">
        <TextBlock Text="Statistics" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <lvc:CartesianChart x:Name="chart" LegendLocation="Right">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis x:Name="axisX" />
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis x:Name="axisY" />
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </StackPanel>
</UserControl>