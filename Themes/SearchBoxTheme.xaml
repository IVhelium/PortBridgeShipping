<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:core="clr-namespace:PortBridgeShipping.Core.DependencyProperties">
    <Style TargetType="{x:Type TextBox}"
           x:Key="SearchBoxTheme">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border Background="{TemplateBinding Background}"
                                Cursor="IBeam"
                                CornerRadius="10"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}">

                            <Grid>
                                <Rectangle StrokeThickness="1" />

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Column="0"
                                           Source="{Binding Path=(core:ImageIconProperties.IconSource), RelativeSource={RelativeSource TemplatedParent}}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Stretch"
                                           Width="24" />

                                    <TextBox Grid.Column="1"
                                             VerticalAlignment="Center"
                                             Background="Transparent"
                                             Foreground="White"
                                             FontFamily="/Fonts/#Cagliostro"
                                             FontSize="14"
                                             Padding="7, 0, 10, 0"
                                             BorderThickness="0"
                                             x:Name="SearchBox" />

                                    <TextBlock Grid.Column="1"
                                               Text="Search"
                                               IsHitTestVisible="False"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Left"
                                               Panel.ZIndex="1"
                                               Margin="10, 0, 0, 0"
                                               Foreground="White"
                                               FontFamily="/Fonts/#Cagliostro"
                                               FontSize="14">

                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}"
                                                                 Value="">
                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>

                                                <Setter Property="Visibility"
                                                        Value="Hidden" />
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                </Grid>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Background"
                    Value="#ff957da9" />
            <Setter Property="BorderThickness"
                    Value="0" />
        </Style.Setters>
    </Style>
</ResourceDictionary>